  /* This table is used by the ODP.NET Buulk Copy sample code. */
  /* Run this script to create the table. Populate it with a few rows. Then, run the ODP.NET sample. */
  CREATE TABLE "BLOGS" ("ID" NUMBER, "URL" VARCHAR2(50) NOT NULL ENABLE);
